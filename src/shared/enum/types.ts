export enum PropertyStatus {
  WAITING = 'waiting',
  DEPLOYED = 'deployed',
  REFUND = 'refund',
  SUCCESS = 'success',
}

export enum PropertyConstructStatus {
  COMPLETE = 'complete',
  INCOMPLETE = 'incomplete',
}
