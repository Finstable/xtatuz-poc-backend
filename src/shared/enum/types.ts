export enum PropertyStatus {
  WAITING = 'waiting',
  DEPLOYED = 'deployed',
  SELLING = 'selling',
  REFUND = 'refund',
  SUCCESS = 'success',
}

export enum PropertyConstructStatus {
  COMPLETE = 'complete',
  INCOMPLETE = 'incomplete',
}
